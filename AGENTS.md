# AGENTS.md

简体中文回答，代码注释中文，commit message 中文。

## 干活方式

单文件改动、查询、解释 —— 自己做完，不要起 subagent。
跨文件改动、影响面不清楚 —— 走 `/plan`，等确认后再 `/implement`。

动手前先说清楚要改哪里、为什么。

## 改动纪律

只改任务要求改的文件。顺手发现的问题写进结论，不要顺手改。
保持文件原有风格，不重构无关代码。
计划和实际代码对不上时停下来说明，不要自行补齐。

## 收尾

说三件事：改了什么、影响谁、要不要测。
验证跑过就贴命令和结果；没跑过直说没跑，不要用"应该没问题"糊过去。
